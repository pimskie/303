{
  "name": "303",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite --config vite.config.js",
    "build": "vite build",
    "preview": "vite preview",
    "prettier": "prettier --write --log-level warn ."
  },
  "devDependencies": {
    "prettier": "3.1.0",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "lit": "^3.1.0"
  }
}
